#include <stdio.h>

int main(){
  puts("Explore data type sizes in unit of byte:");
  printf("* sizeof char: %lu\n", sizeof(char));
  printf("* sizeof int: %lu\n", sizeof(int));
  printf("* sizeof short: %lu\n", sizeof(short));
  printf("* sizeof long: %lu\n", sizeof(long));

  return 0;
}
